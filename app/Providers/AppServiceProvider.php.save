<?php

namespace App\Providers;

use App\Users;
use Illuminate\Support\Facades\Schema;
use Illuminate\Support\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    /**
     * Register any application services.
     *
     * @return void
     */
    public function register()
    {
        $this->app->bind('path.public', function() {
            return base_path().'/public_html';
        });
    }

    /**
     * Bootstrap any application services.
     *
     * @return void
     */
    public function boot()
    {
        
        $image = asset('images/default.jpg');
        if ( $user = Users::find(1)->first() ) {
            $image = asset('images/'.$user->image);
        }
        /*$title = (isset($title)) ? $title : 'Blogue';
        if ( seo()->site(config('app.name')) )
            seo()
                ->site(config('app.name'))
                ->title(
                    default: config("app.name"),
                    modify: fn (string $title) => $title . ' - ' . config("app.name"),
                )
                ->description(default: config("app.description"))

                ->image(default: fn () => $image);*/
        Schema::defaultStringLength(191);

    }
}
